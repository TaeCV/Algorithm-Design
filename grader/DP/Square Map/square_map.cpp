#include <bits/stdc++.h>

using namespace std;

int R, C, M[1005][1005];

void process(int i, int j, int k) {
}

int main() {
    cin >> R >> C;
    for (int i = 1; i <= R; ++i) {
        for (int j = 1; j <= C; ++j) {
            cin >> M[i][j];
        }
    }
    process(1, 1, 0);
}