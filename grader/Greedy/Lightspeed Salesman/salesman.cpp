#include <bits/stdc++.h>

using namespace std;

int N, M, x, y, S[1005], T[1005];

int main() {
    cin >> N >> M;
    for (int i = 0; i < M; ++i)
        cin >> S[i];
    for (int i = 0; i < M; ++i)
        cin >> T[i];
    while (N--) {
        cin >> x >> y;
    }
}